{
  "name": "Users",
  "rowIndex": 0,
  "columnIndex": 0,
  "column": [
    {
      "headerExcel": "Username",
      "field": "username",
      "required": true,
      "unique": true,
      "type": "STRING",
      "regex": "^[a-zA-Z0-9_]{3,20}$",
      "regexErrorMessage": "Username must be 3-20 characters long and contain only letters, numbers, and underscores."
    },
    {
      "headerExcel": "Password",
      "field": "password",
      "required": true,
      "type": "STRING",
      "regex": "^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)(?=.*[@$!%*?&])[A-Za-z\\d@$!%*?&]{8,}$",
      "regexErrorMessage": "Password must be at least 8 characters long and contain at least one uppercase letter, one lowercase letter, one number, and one special character."
    },
    {
      "headerExcel": "Name",
      "field": "name",
      "required": true,
      "type": "STRING",
      "regex": "^[a-zA-Z\\s-']{2,100}$",
      "regexErrorMessage": "Name must be 2-100 characters long and contain only letters, spaces, hyphens, and apostrophes."
    },
    {
      "headerExcel": "Email",
      "field": "email",
      "required": true,
      "unique": true,
      "type": "EMAIL"
    },
    {
      "headerExcel": "Phone Number",
      "field": "phoneNumber",
      "type": "PHONE"
    },
    {
      "headerExcel": "Gender",
      "field": "gender",
      "required": false,
      "type": "STRING",
      "regex": "^(MALE|FEMALE|OTHER)$",
      "regexErrorMessage": "Gender must be one of: MALE, FEMALE, OTHER"
    },
    {
      "headerExcel": "Age",
      "field": "age",
      "required": false,
      "type": "NUMBER"
    },
    {
      "headerExcel": "Date of Birth",
      "field": "dob",
      "required": false,
      "type": "DATE",
      "format": "yyyy-MM-dd"
    },
    {
      "headerExcel": "Code",
      "field": "code",
      "required": false,
      "unique": true,
      "type": "STRING"
    }
  ]
}